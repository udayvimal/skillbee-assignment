# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Configuration & validation
pydantic==2.9.2
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0

# AI services
groq==0.11.0
edge-tts==6.1.12
gTTS==2.5.3

# Retrieval — pure-Python keyword similarity (no ML model, fits Render Free Tier)
# fastembed / faiss-cpu removed: 300 MB ONNX model exceeded 512 MB Render RAM limit

# Utilities
httpx==0.27.2
python-dotenv==1.0.1
aiofiles==24.1.0

# Note: Pillow removed — not used in application code
